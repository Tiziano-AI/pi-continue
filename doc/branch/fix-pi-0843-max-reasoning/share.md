# 协作记录

- 分支：`fix/pi-0843-max-reasoning`
- 目标：解决 Issue #22，使 Pi 0.84.3 返回的 `max` 推理档位可选择、可执行并可持久化，同时保留 Pi 0.74+ 兼容范围。
- 实现：集中定义推理档位及校验规则；设置面板按宿主返回的模型能力筛选；在 Pi 0.74 类型边界内适配 0.84.3 的 `max` provider 请求；不支持 `max` 的旧宿主按现有能力降级。
- 验证：Pi 0.74 类型检查零诊断；254 个测试全部通过；JSON 校验和 npm 打包干跑通过；Pi 0.84.3 compat 声明类型检查零诊断；真实 0.84.3 bundle 探针确认设置选项包含 `max`、provider 收到 `reasoning: "max"`、摘要成功且仅调用一次。
